# This file is a template, and might need editing before it works on your project.
# Full project: https://gitlab.com/pages/jekyll
image: ruby:2.3

pages:
  stage: test
  script:
  - apt-get update
  - apt-get install -y locales
  - echo "en_US UTF-8" > /etc/locale.gen
  - locale-gen en_US.UTF-8
  - export LANG=en_US.UTF-8
  - export LANGUAGE=en_US:en
  - export LC_ALL=en_US.UTF-8
  - apt-get install -y zlib1g-dev
  - gem install jekyll bundler
  - bundle install
  - 'echo url: https://$CI_PROJECT_NAMESPACE.gitlab.io > userconfig.yml'
  - 'echo baseurl: /$CI_PROJECT_NAME/~/$CI_COMMIT_REF_NAME >> userconfig.yml'
  - bundle exec jekyll build -d public/~/$CI_COMMIT_REF_NAME --config _config.yml,userconfig.yml --verbose --trace
  artifacts:
    expire_in: 1 month
    paths:
    - public/~/$CI_COMMIT_REF_NAME
  except:
  - master

pages-deploy:
  stage: deploy
  script:
  - apt-get update
  - apt-get install -y locales
  - echo "en_US UTF-8" > /etc/locale.gen
  - locale-gen en_US.UTF-8
  - export LANG=en_US.UTF-8
  - export LANGUAGE=en_US:en
  - export LC_ALL=en_US.UTF-8
  - apt-get install -y zlib1g-dev
  - gem install jekyll bundler
  - bundle install
  - 'echo url: https://$CI_PROJECT_NAMESPACE.gitlab.io > userconfig.yml'
  - 'echo baseurl: /$CI_PROJECT_NAME >> userconfig.yml'
  - bundle exec jekyll build -d public --config _config.yml,userconfig.yml
  artifacts:
    paths:
    - public
  only:
  - master
